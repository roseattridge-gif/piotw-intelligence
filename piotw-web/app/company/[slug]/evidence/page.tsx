import { notFound } from "next/navigation"; import { CompanyNav } from "@/components/company-nav"; import { EvidenceWall } from "@/components/evidence-wall"; import { getCompanyBySlug } from "@/lib/data/companies";
export default async function EvidencePage({params}:{params:Promise<{slug:string}>}) {const {slug}=await params;const d=await getCompanyBySlug(slug);if(!d)notFound();return <><CompanyNav slug={slug} active="Evidence"/><section className="subpage evidence-section section"><div className="page-intro"><p className="eyebrow">Evidence wall</p><h2>Every observation. Its source. Its limits.</h2><p>Scan the assembled evidence by theme. Open any observation to inspect exactly where it came from and what—if anything—PIOTW interprets from it.</p></div><EvidenceWall evidence={d.evidence} documents={d.documents}/></section></>}
