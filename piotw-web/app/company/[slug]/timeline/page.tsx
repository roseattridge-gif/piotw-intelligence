import { notFound } from "next/navigation"; import { CompanyNav } from "@/components/company-nav"; import { Timeline } from "@/components/timeline"; import { getCompanyBySlug } from "@/lib/data/companies";
export default async function TimelinePage({params}:{params:Promise<{slug:string}>}) {const {slug}=await params;const d=await getCompanyBySlug(slug);if(!d)notFound();return <><CompanyNav slug={slug} active="Timeline"/><section className="subpage section"><div className="page-intro"><p className="eyebrow">Operational timeline</p><h2>How the disclosed story changed</h2><p>Follow the progression of operational change. Every event remains connected to the evidence and source document behind it.</p></div><Timeline events={d.timeline} evidence={d.evidence} documents={d.documents}/></section></>}
