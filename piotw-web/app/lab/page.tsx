import Link from "next/link";import {LabModeNav} from "@/components/lab-mode-nav";import {labEntities,labSnapshots,labSources,labObservations} from "@/lib/data/real-company-lab/travis-perkins";
export default function LabHome(){const group=labEntities[0];const latest=labSnapshots.at(-1)!;return <div className="lab-shell"><LabModeNav/><header className="lab-home-hero"><p className="eyebrow">Real Company Lab v1</p><h1>Can public evidence reconstruct<br/>the operating reality?</h1><p>One company. Seven historical checkpoints. No insider knowledge in the evidence chain.</p></header><section className="lab-home-card"><div><span>01</span><p className="eyebrow">Active reconstruction</p><h2>{group.name}</h2><p>{latest.story}</p><dl><div><dt>Historical checkpoints</dt><dd>{labSnapshots.length}</dd></div><div><dt>Primary sources</dt><dd>{labSources.length}</dd></div><div><dt>Atomic observations</dt><dd>{labObservations.length}</dd></div><div><dt>Latest as of</dt><dd>{latest.asOf}</dd></div></dl></div><Link href="/lab/travis-perkins">Open reconstruction →</Link></section><section className="lab-queue"><p className="eyebrow">Entity architecture</p><h2>Group evidence is not business-unit evidence.</h2><div>{labEntities.map(entity=><article key={entity.id}><strong>{entity.name}</strong><span>{entity.kind.replace("_"," ")}</span><small>{entity.status==="IN_SCOPE"?"Active lab":"Prepared for later"}</small></article>)}</div></section></div>}
